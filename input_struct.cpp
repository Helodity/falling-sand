#include "input_struct.h"

input_struct::input_struct():
left_mouse_down(false),
right_mouse_down(false),
scroll_wheel_change(0),
mouse_pos(point(0,0)){
}

user_stuff::user_stuff():
place_radius(10),
selected_id(3){
}